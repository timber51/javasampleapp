pipeline {
    agent {node 'master'}
    stages {
        stage('Build') { 
            steps {
                echo "This branch is for peer review and dev env only."
            }
        }
    }
}
